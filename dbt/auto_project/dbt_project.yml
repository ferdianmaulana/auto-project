name: auto_project
version: '1.0.0'
config-version: 2

profile: auto_project

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  auto_project:
    staging:
      +materialized: view
      +schema: staging
    consumption:
      +materialized: table
      +schema: consumption